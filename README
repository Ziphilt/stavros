Stavros, a physics/graphics/game project by Cal Stepanian

Compile with compile.sh

Dependencies
  SFML 1.6
  OpenGL
  Chipmunk
    depends:
      libxmu-dev
      libxi-dev
      GLUT
      cmake
    install:
      git clone https://github.com/slembcke/Chipmunk-Physics.git
      cmake .
      make
      sudo make install
      sudo ldconfig
